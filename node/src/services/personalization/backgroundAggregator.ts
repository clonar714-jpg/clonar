/**
 * Minimal stub so the server can start when running query-pipeline only.
 * Replace with real implementation if you need personalization background jobs.
 */
export function startBackgroundJob(): void {
  // no-op
}
