/**
 * ✅ Server Registry: Server-side configuration accessors
 * Alias for config accessors (matches the expected import path)
 */

export { getConfiguredModelProviders, getConfiguredModelProviderById, getSearxngURL } from './accessors';

