/**
 * Minimal stub so the server can start when running query-pipeline only.
 * Replace with real implementation if you need periodic memory flush.
 */
export function startMemoryFlushScheduler(): void {
  // no-op
}
